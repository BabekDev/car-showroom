export interface FilterProps {
    filterMark: string | null;
    filterModel: string | null;
    filterColor: string | null;
    initialMark?: string;    
    initialModel?: string;   
    initialColor?: string;
}